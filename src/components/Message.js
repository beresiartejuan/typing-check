function Message({ className }){

    return (
        <div className={`text-center text-gray-300 mx-auto ${className}`}><p>
            ¡Presione cualquier tecla para empezar!
        </p></div>
    )

}

export default Message;